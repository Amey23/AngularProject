import { TabledirDirective } from './tabledir.directive';

describe('TabledirDirective', () => {
  it('should create an instance', () => {
    const directive = new TabledirDirective();
    expect(directive).toBeTruthy();
  });
});
